---
import Layout from '../layouts/Layout.astro';
import { api } from '../lib/api';

const featuredHotels   = await api.getFeaturedHotels().catch(() => []);
const featuredTours    = await api.getFeaturedTours().catch(() => []);
const featuredTransfers = await api.getFeaturedTransfers().catch(() => []);
const banners          = await api.getBanners('home').catch(() => []);

const heroTitle = banners[0]?.title || 'Tu paraíso,<br>reservado.';
const heroSubtitle = banners[0]?.subtitle || 'Tours, hoteles y traslados en Cancún, Riviera Maya y el Caribe Mexicano';

const homeSchema = {
  '@context': 'https://schema.org',
  '@type': 'TouristDestination',
  name: 'Riviera Maya & Cancún',
  description: 'Destino turístico de clase mundial en el Caribe Mexicano con playas, ruinas mayas y vida marina excepcional',
  touristType: ['Families', 'Couples', 'Solo Travelers'],
  geo: {
    '@type': 'GeoCoordinates',
    latitude: 20.6534,
    longitude: -87.0696,
  },
};
---

<Layout
  title="Booking Caribe - Tours, Hoteles y Traslados en Riviera Maya & Cancún"
  description="Reserva tours, hoteles y traslados en Cancún, Playa del Carmen, Tulum y Riviera Maya. Mejores precios garantizados. ¡Más de 5,000 viajeros satisfechos!"
  keywords="tours cancun, hoteles riviera maya, traslado aeropuerto cancun, booking caribe, tours playa del carmen, excursiones tulum, tours cozumel, tours chichen itza"
  schema={homeSchema}
  transparent={true}
>

  <!-- ══════════════════════════════════════════
       HERO SECTION
  ══════════════════════════════════════════ -->
  <section class="hero-ocean min-h-screen flex flex-col" aria-label="Inicio">

    <!-- Background decorative elements -->
    <div class="absolute inset-0 overflow-hidden pointer-events-none" aria-hidden="true">
      <!-- Floating circles -->
      <div class="absolute top-1/4 right-1/5 w-64 h-64 rounded-full bg-cyan-400/5 blur-3xl float" style="animation-delay: 0s"></div>
      <div class="absolute bottom-1/3 left-1/6 w-48 h-48 rounded-full bg-orange-400/5 blur-3xl float" style="animation-delay: 2s"></div>
      <div class="absolute top-1/2 left-1/2 w-96 h-96 rounded-full bg-primary/4 blur-3xl -translate-x-1/2 -translate-y-1/2 spin-slow"></div>
    </div>

    <!-- Hero Content -->
    <div class="flex-1 flex items-center relative z-10">
      <div class="container mx-auto px-4 pt-24 pb-16">
        <div class="max-w-4xl mx-auto text-center">

          <!-- Badge -->
          <div class="fade-in-hero-1 inline-flex items-center gap-2 bg-white/10 border border-white/20 rounded-full px-4 py-1.5 text-white/85 text-sm font-medium mb-6 backdrop-blur-sm">
            <span class="w-1.5 h-1.5 rounded-full bg-primary animate-pulse"></span>
            Cancún · Riviera Maya · Caribe Mexicano
          </div>

          <!-- Main Headline -->
          <h1 class="fade-in-hero-2 font-display text-5xl sm:text-6xl md:text-7xl font-bold text-white leading-[1.05] tracking-tight mb-6" set:html={heroTitle}>
          </h1>

          <!-- Subtitle -->
          <p class="fade-in-hero-3 text-lg sm:text-xl text-white/75 max-w-2xl mx-auto leading-relaxed mb-10">
            {heroSubtitle}
          </p>

          <!-- Category Buttons -->
          <div class="fade-in-hero-4 flex flex-wrap justify-center gap-3 mb-12">
            <a
              href="/hotels"
              class="flex items-center gap-2.5 bg-white text-neutral font-bold px-7 py-3.5 rounded-2xl shadow-lg hover:shadow-xl hover:-translate-y-0.5 transition-all duration-200 text-sm"
            >
              <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-primary" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6" />
              </svg>
              Hoteles
            </a>
            <a
              href="/tours"
              class="flex items-center gap-2.5 bg-primary text-white font-bold px-7 py-3.5 rounded-2xl shadow-lg shadow-primary/30 hover:shadow-xl hover:shadow-primary/40 hover:-translate-y-0.5 transition-all duration-200 text-sm"
            >
              <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z" />
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z" />
              </svg>
              Tours & Actividades
            </a>
            <a
              href="/transfers"
              class="flex items-center gap-2.5 bg-secondary text-white font-bold px-7 py-3.5 rounded-2xl shadow-lg shadow-secondary/30 hover:shadow-xl hover:shadow-secondary/40 hover:-translate-y-0.5 transition-all duration-200 text-sm"
            >
              <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7h12m0 0l-4-4m4 4l-4 4m0 6H4m0 0l4 4m-4-4l4-4" />
              </svg>
              Traslados
            </a>
          </div>

          <!-- Trust Badges -->
          <div class="fade-in-hero-4 flex flex-wrap justify-center gap-3">
            <div class="trust-badge">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 text-yellow-300" viewBox="0 0 20 20" fill="currentColor">
                <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z" />
              </svg>
              4.9 · Más de 5,000 viajeros
            </div>
            <div class="trust-badge">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 text-green-300" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z" />
              </svg>
              Cancelación gratuita
            </div>
            <div class="trust-badge">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 text-blue-300" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
              </svg>
              Atención 24/7
            </div>
            <div class="trust-badge">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 text-cyan-300" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
              </svg>
              Mejor precio garantizado
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Animated Wave Bottom -->
    <div class="hero-waves relative z-10" aria-hidden="true">
      <svg viewBox="0 0 1440 80" xmlns="http://www.w3.org/2000/svg" preserveAspectRatio="none">
        <path fill="#fffbf7" d="M0,40 C180,80 360,0 540,40 C720,80 900,0 1080,40 C1260,80 1380,20 1440,40 L1440,80 L0,80 Z" />
        <path fill="#fffbf7" d="M0,60 C240,30 480,80 720,50 C960,20 1200,70 1440,55 L1440,80 L0,80 Z" opacity="0.5"/>
      </svg>
    </div>

    <!-- Scroll Indicator -->
    <div class="absolute bottom-24 left-1/2 -translate-x-1/2 z-10 hidden md:block" aria-hidden="true">
      <div class="scroll-indicator opacity-60"></div>
    </div>
  </section>

  <!-- ══════════════════════════════════════════
       STATS / QUICK NUMBERS
  ══════════════════════════════════════════ -->
  <section class="bg-base-100 py-12" aria-label="Estadísticas">
    <div class="container mx-auto px-4">
      <div class="grid grid-cols-2 md:grid-cols-4 gap-6 max-w-4xl mx-auto">
        {[
          { number: '200+', label: 'Tours disponibles', icon: '🌴' },
          { number: '150+', label: 'Hoteles seleccionados', icon: '🏨' },
          { number: '50+', label: 'Rutas de traslado', icon: '🚐' },
          { number: '5,000+', label: 'Viajeros satisfechos', icon: '⭐' },
        ].map(({ number, label, icon }, i) => (
          <div class={`reveal-up delay-${(i + 1) * 100} text-center`}>
            <div class="text-3xl mb-1" aria-hidden="true">{icon}</div>
            <div class="text-3xl font-bold text-primary stat-number">{number}</div>
            <div class="text-sm text-base-content/60 mt-1">{label}</div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- ══════════════════════════════════════════
       CATEGORY CARDS
  ══════════════════════════════════════════ -->
  <section class="bg-base-200 py-20" aria-label="Categorías de servicios">
    <div class="container mx-auto px-4">
      <div class="text-center mb-12">
        <p class="reveal-up text-sm font-bold uppercase tracking-widest text-primary mb-3">Nuestros Servicios</p>
        <h2 class="reveal-up delay-100 text-4xl md:text-5xl font-bold text-base-content">¿Qué estás buscando?</h2>
        <p class="reveal-up delay-200 text-base-content/60 mt-4 max-w-xl mx-auto">
          Desde traslados al aeropuerto hasta tours exclusivos — tenemos todo lo que necesitas para vivir el Caribe.
        </p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
        <!-- Hotels -->
        <a href="/hotels" class="cat-card reveal-scale delay-100 h-72 block" aria-label="Ver hoteles">
          <div class="absolute inset-0 bg-gradient-to-br from-sky-500 via-cyan-600 to-teal-700"></div>
          <div class="absolute inset-0 opacity-20"
            style="background-image: url('data:image/svg+xml;charset=utf-8,<svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"0 0 100 100\"><circle cx=\"50\" cy=\"50\" r=\"40\" fill=\"none\" stroke=\"white\" stroke-width=\"0.5\" opacity=\"0.3\"/><circle cx=\"50\" cy=\"50\" r=\"25\" fill=\"none\" stroke=\"white\" stroke-width=\"0.5\" opacity=\"0.5\"/></svg>');">
          </div>
          <div class="cat-overlay"></div>
          <div class="cat-content">
            <div class="text-4xl mb-2" aria-hidden="true">🏨</div>
            <h3 class="text-2xl font-bold text-white">Hoteles</h3>
            <p class="text-white/75 text-sm mt-1">Los mejores alojamientos de la Riviera Maya</p>
            <div class="mt-3 inline-flex items-center gap-1.5 text-cyan-200 text-sm font-semibold">
              Explorar hoteles
              <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
              </svg>
            </div>
          </div>
        </a>

        <!-- Tours -->
        <a href="/tours" class="cat-card reveal-scale delay-200 h-72 block" aria-label="Ver tours">
          <div class="absolute inset-0 bg-gradient-to-br from-emerald-500 via-green-600 to-teal-800"></div>
          <div class="cat-overlay"></div>
          <div class="cat-content">
            <div class="text-4xl mb-2" aria-hidden="true">🌴</div>
            <h3 class="text-2xl font-bold text-white">Tours & Actividades</h3>
            <p class="text-white/75 text-sm mt-1">Experiencias únicas en el Caribe Mexicano</p>
            <div class="mt-3 inline-flex items-center gap-1.5 text-green-200 text-sm font-semibold">
              Ver tours
              <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
              </svg>
            </div>
          </div>
        </a>

        <!-- Transfers -->
        <a href="/transfers" class="cat-card reveal-scale delay-300 h-72 block" aria-label="Ver traslados">
          <div class="absolute inset-0 bg-gradient-to-br from-orange-500 via-amber-600 to-red-700"></div>
          <div class="cat-overlay"></div>
          <div class="cat-content">
            <div class="text-4xl mb-2" aria-hidden="true">🚐</div>
            <h3 class="text-2xl font-bold text-white">Traslados</h3>
            <p class="text-white/75 text-sm mt-1">Privados y compartidos desde el aeropuerto</p>
            <div class="mt-3 inline-flex items-center gap-1.5 text-orange-200 text-sm font-semibold">
              Ver traslados
              <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
              </svg>
            </div>
          </div>
        </a>
      </div>
    </div>
  </section>

  <!-- Wave divider -->
  <div class="wave-divider bg-base-200" aria-hidden="true">
    <svg viewBox="0 0 1440 60" xmlns="http://www.w3.org/2000/svg" preserveAspectRatio="none">
      <path fill="#fffbf7" d="M0,30 C360,60 720,0 1080,30 C1260,45 1380,15 1440,30 L1440,60 L0,60 Z"/>
    </svg>
  </div>

  <!-- ══════════════════════════════════════════
       FEATURED TOURS
  ══════════════════════════════════════════ -->
  <section class="bg-base-100 py-20" aria-label="Tours destacados">
    <div class="container mx-auto px-4">
      <div class="flex flex-col md:flex-row items-start md:items-end justify-between mb-12 gap-4">
        <div>
          <p class="reveal-up text-sm font-bold uppercase tracking-widest text-secondary mb-2">Tours Destacados</p>
          <h2 class="reveal-up delay-100 text-4xl md:text-5xl font-bold">Experiencias que cambian vidas</h2>
          <p class="reveal-up delay-200 text-base-content/60 mt-3 max-w-lg">
            Chichén Itzá, cenotes, Cozumel, Tulum… cada tour es una historia que contar.
          </p>
        </div>
        <a href="/tours" class="reveal-up delay-300 btn btn-outline btn-primary shrink-0">
          Ver todos los tours
          <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
          </svg>
        </a>
      </div>

      {featuredTours.length > 0 ? (
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-7">
          {featuredTours.slice(0, 6).map((tour, i) => (
            <article class={`card bg-base-100 shadow-md border border-base-200 card-lift reveal-up delay-${Math.min((i % 3 + 1) * 100, 400)} overflow-hidden`}>
              <!-- Image placeholder with gradient -->
              <figure class="h-52 relative overflow-hidden">
                <div class="absolute inset-0 bg-gradient-to-br from-emerald-400 to-teal-600"></div>
                <div class="absolute inset-0 flex items-center justify-center">
                  <svg xmlns="http://www.w3.org/2000/svg" class="h-20 w-20 text-white/30" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z" />
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z" />
                  </svg>
                </div>
                {tour.featured && (
                  <div class="absolute top-3 right-3 badge badge-hot text-xs px-2.5 py-1">Destacado</div>
                )}
                <div class="absolute top-3 left-3">
                  <span class="badge badge-neutral text-xs capitalize">
                    {tour.difficulty_level === 'easy' ? 'Fácil' : tour.difficulty_level === 'moderate' ? 'Moderado' : 'Difícil'}
                  </span>
                </div>
              </figure>

              <div class="card-body p-5">
                <h3 class="font-bold text-lg leading-tight line-clamp-2">{tour.name}</h3>
                <p class="text-sm text-base-content/60 line-clamp-2 mt-1">{tour.description}</p>

                <div class="flex flex-wrap gap-3 mt-3 text-xs text-base-content/55">
                  <span class="flex items-center gap-1">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-3.5 w-3.5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
                    </svg>
                    {tour.duration}
                  </span>
                  <span class="flex items-center gap-1">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-3.5 w-3.5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0z" />
                    </svg>
                    Hasta {tour.max_people} personas
                  </span>
                </div>

                <div class="flex items-center justify-between mt-4 pt-4 border-t border-base-200">
                  <div>
                    <p class="text-xs text-base-content/50">Desde</p>
                    <p class="text-2xl font-bold price-gradient">
                      ${tour.price_adult.toLocaleString('es-MX')}
                      <span class="text-xs font-normal text-base-content/50 ml-0.5">MXN</span>
                    </p>
                  </div>
                  <a href={`/tours/${tour.slug}`} class="btn btn-primary btn-sm px-5">
                    Reservar
                  </a>
                </div>
              </div>
            </article>
          ))}
        </div>
      ) : (
        <div class="text-center py-16 text-base-content/40">
          <p class="text-6xl mb-4">🌴</p>
          <p class="text-xl font-semibold">Tours próximamente disponibles</p>
        </div>
      )}
    </div>
  </section>

  <!-- Wave Divider -->
  <div class="wave-divider bg-base-100" aria-hidden="true">
    <svg viewBox="0 0 1440 60" xmlns="http://www.w3.org/2000/svg" preserveAspectRatio="none">
      <path fill="#f0f9ff" d="M0,20 C360,60 720,0 1080,30 C1260,45 1380,10 1440,20 L1440,60 L0,60 Z"/>
    </svg>
  </div>

  <!-- ══════════════════════════════════════════
       FEATURED HOTELS
  ══════════════════════════════════════════ -->
  <section class="bg-base-200 py-20" aria-label="Hoteles recomendados">
    <div class="container mx-auto px-4">
      <div class="text-center mb-12">
        <p class="reveal-up text-sm font-bold uppercase tracking-widest text-primary mb-2">Hoteles Recomendados</p>
        <h2 class="reveal-up delay-100 text-4xl md:text-5xl font-bold">Alojamientos de ensueño</h2>
        <p class="reveal-up delay-200 text-base-content/60 mt-3 max-w-lg mx-auto">
          Desde boutiques íntimos hasta resorts all-inclusive con vista al mar Caribe.
        </p>
      </div>

      {featuredHotels.length > 0 ? (
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
          {featuredHotels.slice(0, 4).map((hotel, i) => (
            <article class={`card lg:card-side bg-base-100 shadow-md border border-base-200 card-lift reveal-${i % 2 === 0 ? 'left' : 'right'} delay-${(i % 2 + 1) * 100} overflow-hidden`}>
              <figure class="lg:w-44 h-44 lg:h-auto relative shrink-0">
                <div class="absolute inset-0 bg-gradient-to-br from-sky-400 to-cyan-600"></div>
                <div class="absolute inset-0 flex items-center justify-center">
                  <svg xmlns="http://www.w3.org/2000/svg" class="h-16 w-16 text-white/30" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6" />
                  </svg>
                </div>
              </figure>
              <div class="card-body p-5">
                <div class="flex items-start justify-between gap-2">
                  <h3 class="font-bold text-lg leading-tight">{hotel.name}</h3>
                  <div class="flex shrink-0">
                    {[...Array(hotel.stars)].map(() => (
                      <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 text-amber-400 fill-amber-400" viewBox="0 0 20 20">
                        <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z" />
                      </svg>
                    ))}
                  </div>
                </div>
                <p class="text-sm text-base-content/60 line-clamp-2 mt-1">{hotel.description}</p>
                <div class="flex items-center gap-1.5 text-xs text-base-content/50 mt-2">
                  <svg xmlns="http://www.w3.org/2000/svg" class="h-3.5 w-3.5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z" />
                  </svg>
                  {hotel.city}{hotel.state ? `, ${hotel.state}` : ''}
                </div>
                <div class="flex items-center justify-between mt-4 pt-3 border-t border-base-200">
                  <div>
                    <p class="text-xs text-base-content/50">Por noche desde</p>
                    <p class="text-2xl font-bold price-gradient">
                      ${hotel.price_per_night.toLocaleString('es-MX')}
                    </p>
                  </div>
                  <a href={`/hotels/${hotel.slug}`} class="btn btn-primary btn-sm px-5">
                    Reservar
                  </a>
                </div>
              </div>
            </article>
          ))}
        </div>
      ) : (
        <div class="text-center py-16 text-base-content/40">
          <p class="text-6xl mb-4">🏨</p>
          <p class="text-xl font-semibold">Hoteles próximamente disponibles</p>
        </div>
      )}

      <div class="text-center mt-10">
        <a href="/hotels" class="btn btn-outline btn-lg">
          Ver todos los hoteles
          <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
          </svg>
        </a>
      </div>
    </div>
  </section>

  <!-- Wave Divider -->
  <div class="wave-divider bg-base-200" aria-hidden="true">
    <svg viewBox="0 0 1440 60" xmlns="http://www.w3.org/2000/svg" preserveAspectRatio="none">
      <path fill="#fffbf7" d="M0,40 C480,10 960,60 1440,20 L1440,60 L0,60 Z"/>
    </svg>
  </div>

  <!-- ══════════════════════════════════════════
       FEATURED TRANSFERS
  ══════════════════════════════════════════ -->
  <section class="bg-base-100 py-20" aria-label="Traslados disponibles">
    <div class="container mx-auto px-4">
      <div class="flex flex-col md:flex-row items-start md:items-end justify-between mb-12 gap-4">
        <div>
          <p class="reveal-up text-sm font-bold uppercase tracking-widest text-secondary mb-2">Traslados</p>
          <h2 class="reveal-up delay-100 text-4xl md:text-5xl font-bold">Llega sin estrés</h2>
          <p class="reveal-up delay-200 text-base-content/60 mt-3 max-w-lg">
            Del aeropuerto de Cancún a tu hotel — privado o compartido, puntual y cómodo.
          </p>
        </div>
        <a href="/transfers" class="reveal-up delay-300 btn btn-outline btn-secondary shrink-0">
          Ver todos los traslados
        </a>
      </div>

      {featuredTransfers.length > 0 ? (
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-7">
          {featuredTransfers.slice(0, 3).map((transfer, i) => (
            <article class={`card bg-base-100 shadow-md border border-base-200 card-lift reveal-up delay-${(i + 1) * 100}`}>
              <div class="card-body p-6">
                <div class="flex items-start justify-between gap-2 mb-4">
                  <h3 class="font-bold text-lg leading-tight">{transfer.name}</h3>
                  <span class={`badge badge-sm shrink-0 ${transfer.is_private ? 'badge-primary' : 'badge-accent'}`}>
                    {transfer.is_private ? 'Privado' : 'Compartido'}
                  </span>
                </div>

                <!-- Route visualization -->
                <div class="bg-base-200 rounded-xl p-4 mb-4">
                  <div class="flex items-center gap-2 text-sm">
                    <div class="w-2 h-2 rounded-full bg-success shrink-0"></div>
                    <span class="font-medium truncate">{transfer.from_location}</span>
                  </div>
                  <div class="ml-1 my-1.5 w-0.5 h-4 bg-base-300 ml-[3px]"></div>
                  <div class="flex items-center gap-2 text-sm">
                    <div class="w-2 h-2 rounded-full bg-error shrink-0"></div>
                    <span class="font-medium truncate">{transfer.to_location}</span>
                  </div>
                </div>

                <div class="grid grid-cols-2 gap-3 text-sm text-base-content/60">
                  <span class="flex items-center gap-1.5">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 text-primary" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0z" />
                    </svg>
                    {transfer.max_passengers} pasajeros
                  </span>
                  <span class="flex items-center gap-1.5 capitalize">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 text-primary" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7v8a2 2 0 002 2h6M8 7V5a2 2 0 012-2h4.586a1 1 0 01.707.293l4.414 4.414a1 1 0 01.293.707V15a2 2 0 01-2 2h-2M8 7H6a2 2 0 00-2 2v10a2 2 0 002 2h8a2 2 0 002-2v-2" />
                    </svg>
                    {transfer.vehicle_type}
                  </span>
                </div>

                <div class="flex items-center justify-between mt-5 pt-4 border-t border-base-200">
                  <div>
                    <p class="text-xs text-base-content/50">
                      {transfer.type === 'one-way' ? 'Solo ida' : 'Ida y vuelta'}
                    </p>
                    <p class="text-2xl font-bold price-gradient">
                      ${transfer.price.toLocaleString('es-MX')}
                    </p>
                  </div>
                  <a href={`/transfers/${transfer.slug}`} class="btn btn-secondary btn-sm px-5 text-white">
                    Reservar
                  </a>
                </div>
              </div>
            </article>
          ))}
        </div>
      ) : (
        <div class="text-center py-16 text-base-content/40">
          <p class="text-6xl mb-4">🚐</p>
          <p class="text-xl font-semibold">Traslados próximamente disponibles</p>
        </div>
      )}
    </div>
  </section>

  <!-- ══════════════════════════════════════════
       WHY US
  ══════════════════════════════════════════ -->
  <section class="bg-neutral py-20" aria-label="Por qué elegirnos">
    <div class="container mx-auto px-4">
      <div class="text-center mb-14">
        <p class="reveal-up text-sm font-bold uppercase tracking-widest text-primary mb-3">¿Por qué Booking Caribe?</p>
        <h2 class="reveal-up delay-100 text-4xl md:text-5xl font-bold text-white">
          La diferencia que sientes<br class="hidden md:block"/> desde el primer click
        </h2>
      </div>

      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8">
        {[
          {
            icon: '🛡️',
            title: 'Pago 100% Seguro',
            desc: 'Transacciones protegidas con encriptación SSL. Tu dinero siempre está a salvo.',
            delay: '100',
          },
          {
            icon: '⚡',
            title: 'Confirmación Inmediata',
            desc: 'Recibe tu voucher por email en segundos. Sin esperas ni incertidumbre.',
            delay: '200',
          },
          {
            icon: '🤝',
            title: 'Atención 24/7',
            desc: 'Nuestro equipo está disponible las 24 horas por WhatsApp, email o teléfono.',
            delay: '300',
          },
          {
            icon: '✅',
            title: 'Cancelación Flexible',
            desc: 'Cancela hasta 24-48h antes sin penalización. Planes que se adaptan a ti.',
            delay: '400',
          },
        ].map(({ icon, title, desc, delay }) => (
          <div class={`reveal-up delay-${delay} text-center group`}>
            <div class="w-16 h-16 rounded-2xl bg-white/8 flex items-center justify-center text-3xl mx-auto mb-4 group-hover:bg-primary/20 transition-colors">
              {icon}
            </div>
            <h3 class="font-bold text-white text-lg mb-2">{title}</h3>
            <p class="text-neutral-content/55 text-sm leading-relaxed">{desc}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- ══════════════════════════════════════════
       CTA BANNER
  ══════════════════════════════════════════ -->
  <section
    class="relative overflow-hidden py-24"
    style="background: linear-gradient(135deg, #0891b2 0%, #0e7490 40%, #164e63 100%);"
    aria-label="Oferta especial"
  >
    <!-- Decorative circles -->
    <div class="absolute -top-24 -right-24 w-96 h-96 rounded-full bg-white/5 pointer-events-none" aria-hidden="true"></div>
    <div class="absolute -bottom-24 -left-24 w-80 h-80 rounded-full bg-white/5 pointer-events-none" aria-hidden="true"></div>

    <div class="container mx-auto px-4 text-center relative z-10">
      <div class="reveal-up inline-flex items-center gap-2 bg-white/15 border border-white/25 rounded-full px-4 py-1.5 text-white text-sm font-bold mb-6">
        🎉 Oferta Especial
      </div>
      <h2 class="reveal-up delay-100 text-4xl md:text-5xl font-bold text-white mb-4">
        ¡15% de descuento en tu primera reserva!
      </h2>
      <p class="reveal-up delay-200 text-xl text-white/80 mb-8 max-w-xl mx-auto">
        Usa el código <strong class="text-yellow-300">VERANO2026</strong> al finalizar tu reserva.
      </p>
      <div class="reveal-up delay-300 flex flex-wrap justify-center gap-4">
        <a href="/tours" class="btn btn-lg bg-white text-primary hover:bg-base-100 font-bold px-8 shadow-lg">
          Explorar Tours
        </a>
        <a href="/contact" class="btn btn-lg btn-outline border-white text-white hover:bg-white hover:text-primary font-bold px-8">
          Contactar
        </a>
      </div>
    </div>
  </section>

</Layout>
